//Conversion larga 
var celsius = prompt ("Introduzca la temperatura en grados celsius");
celsius = parseFloat (celsius);

// conversion corta
// var celsius = parseFloat (prompt ("Introduzca la temperatura en grados cdelsius"));
var f= 9/5 *celsius+32;
document.write ("La conversion es:", f);